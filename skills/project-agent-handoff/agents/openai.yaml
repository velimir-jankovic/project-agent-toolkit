interface:
  display_name: "Project Agent Handoff"
  short_description: "Create compact resumable project checkpoints"
  default_prompt: "Use $project-agent-handoff to checkpoint or resume this project cleanly."
policy:
  allow_implicit_invocation: true
