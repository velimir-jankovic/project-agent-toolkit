#!/usr/bin/env python
"""Run the repository's authoritative quality gate before commit."""

from __future__ import annotations

import subprocess
import sys


raise SystemExit(
    subprocess.run(
        [sys.executable, "scripts/quality.py"],
        check=False,
    ).returncode
)
