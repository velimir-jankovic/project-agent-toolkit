# Architecture

Record stable system boundaries, ownership, public contracts, and accepted
design decisions here. Keep implementation diaries and temporary plans out of
this document.

## System boundaries

Document the major components and the direction of their dependencies.

## Ownership

For each reusable capability, name the component that owns its contract,
implementation, validation, and lifecycle.

## Decisions

Record decisions whose rationale must survive the current task.
